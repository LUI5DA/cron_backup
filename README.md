# Back Up tool with cron


A simple backup tool that uses cron jobs to automate the backup process of specified directories or files.

## Features

- Stores the back up data in azure blob storage.

- Automates the backup process using cron jobs.



